apiVersion: v1
kind: Secret
metadata:
  name: api-gateway-secrets
  namespace: movenow
  labels:
    app: api-gateway
type: Opaque
stringData:
  # IMPORTANT: These are example values. Replace with actual secrets in production.
  # Use sealed-secrets, external-secrets, or vault for production deployments.
  JWT_ACCESS_SECRET: "REPLACE_WITH_SECURE_ACCESS_SECRET"
  JWT_REFRESH_SECRET: "REPLACE_WITH_SECURE_REFRESH_SECRET"
