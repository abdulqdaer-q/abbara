# This is an example secrets file. DO NOT commit actual secrets to git.
# Create actual secrets using kubectl or a secrets management tool.
#
# kubectl create secret generic realtime-gateway-secrets \
#   --from-literal=JWT_ACCESS_SECRET=your-access-secret \
#   --from-literal=JWT_SOCKET_SECRET=your-socket-secret \
#   --from-literal=REDIS_PASSWORD=your-redis-password

apiVersion: v1
kind: Secret
metadata:
  name: realtime-gateway-secrets
type: Opaque
stringData:
  JWT_ACCESS_SECRET: "CHANGE_ME_IN_PRODUCTION"
  JWT_SOCKET_SECRET: "CHANGE_ME_IN_PRODUCTION"
  REDIS_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
