# This is a template - DO NOT commit actual secrets to git
# Use kubectl create secret or a secrets management solution like Sealed Secrets
apiVersion: v1
kind: Secret
metadata:
  name: porters-secrets
type: Opaque
stringData:
  database-url: "postgresql://USER:PASSWORD@postgres-service:5432/movenow_porters"
  database-replica-url: "postgresql://USER:PASSWORD@postgres-replica:5432/movenow_porters"
  jwt-secret: "CHANGE_THIS_IN_PRODUCTION"
  admin-api-key: "CHANGE_THIS_IN_PRODUCTION"
